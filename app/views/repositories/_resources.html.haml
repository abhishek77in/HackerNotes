%section{id: id, class: 'clearfix'}
  %a{href: show_resources_path(owner: @repo.owner, name: @repo.name, resource: id)}
    %h2= title
  - resources.each do |r|
    - next if r.title.nil?
    %div.resource
      %h3.title
        = link_to(r.title.html_safe, r.url)
        %span.voteContainer.hidden
          = link_to_vote(r, :up)
          %span= r.votes_count
          = link_to_vote(r, :down)
      %p
        = r.description.html_safe if r.description.present?
%hr
